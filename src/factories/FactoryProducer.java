package factories;

import entities.Bike;
import entities.Doll;
import entities.Toy;

public class FactoryProducer {


    /*public static <T> AbstractFactory<T> getFactory(String factoryName) {
        if (factoryName.equals("BIKE")) {
            return (AbstractFactory<Bike>) new BikeFactory();
        } else if (factoryName.equals("DOLL")) {
            return (AbstractFactory<Doll>) new DollFactory();
        } else {
            System.out.println("unknown factory");
            return null;
        }
    }*/
}
